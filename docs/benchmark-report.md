# XAUDIT Benchmark Report

> **What this is, and is not.** This report is computed against the project's own regression corpus (`tests/fixtures/`), which was written alongside the rules being tested. A precision/recall of 1.00 here means "the engine still behaves the way its own test suite says it should" — it is a regression signal, not an independent measurement of real-world accuracy. It says nothing about code this corpus doesn't cover. Do not cite this report as evidence of general detection accuracy, "zero false positives" in the wild, or coverage of any vulnerability class beyond the five rule groups implemented (see README "Current supported checks"). Real-world code will produce both false positives and false negatives this corpus does not exercise.

Generated by `npm run bench` (`scripts/run-benchmark.ts`) against `tests/expected-results.json` and the live `analyze()` engine. This is not a curated or cherry-picked sample in the sense of hiding failures — it is the exact corpus committed under `tests/fixtures/`, the same one the CI-failing test suite in `tests/` runs against, including cases deliberately designed to demonstrate known blind spots (see below).

Generated: 2026-09-03T06:43:26.535Z

## Overall

| Cases | TP | FP | FN | TN | Precision | Recall | F1 |
|---|---|---|---|---|---|---|---|
| 98 | 48 | 0 | 0 | 50 | 1.00 | 1.00 | 1.00 |

## By rule group

| Group | Cases | TP | FP | FN | TN | Precision | Recall | F1 |
|---|---|---|---|---|---|---|---|---|
| xss | 24 | 14 | 0 | 0 | 10 | 1.00 | 1.00 | 1.00 |
| sqli | 20 | 13 | 0 | 0 | 7 | 1.00 | 1.00 | 1.00 |
| secrets | 17 | 9 | 0 | 0 | 8 | 1.00 | 1.00 | 1.00 |
| exec | 10 | 6 | 0 | 0 | 4 | 1.00 | 1.00 | 1.00 |
| node-command | 5 | 3 | 0 | 0 | 2 | 1.00 | 1.00 | 1.00 |
| react-regression | 5 | 0 | 0 | 0 | 5 | 1.00 | 1.00 | 1.00 |
| auth | 12 | 3 | 0 | 0 | 9 | 1.00 | 1.00 | 1.00 |
| general-safe | 5 | 0 | 0 | 0 | 5 | 1.00 | 1.00 | 1.00 |

## Case-by-case detail

| ID | Group | Bucket | Outcome | Findings raised | Note |
|---|---|---|---|---|---|
| xss-inner-html-user-input | xss | vulnerable | TP | xss-inner-html | raw innerHTML from parameter |
| xss-outer-html-alias | xss | vulnerable | TP | xss-outer-html | outerHTML via aliased element |
| xss-insert-adjacent-html | xss | vulnerable | TP | xss-insert-adjacent-html | insertAdjacentHTML with dynamic value |
| xss-document-write | xss | vulnerable | TP | xss-document-write | document.write with template interpolation |
| xss-document-writeln | xss | vulnerable | TP | xss-document-writeln | document.writeln with concatenation |
| xss-dangerously-set-inner-html | xss | vulnerable | TP | xss-dangerously-set-inner-html | React dangerouslySetInnerHTML, unsanitized |
| xss-template-literal-inner-html | xss | vulnerable | TP | xss-inner-html | template literal built into innerHTML |
| xss-sanitized-dompurify | xss | safe | TN | xss-inner-html | sanitized via DOMPurify before innerHTML (should be low, not high) |
| xss-static-literal-inner-html | xss | safe | TN | (none) | constant literal HTML, no dynamic value at all |
| xss-literal-via-one-hop-variable | xss | safe | TN | (none) | a literal assigned to a variable one step before the sink — fixed false positive: this used to be flagged (the identifier itself is never a literal by syntax) even though the traced value is a plain string |
| xss-sanitizer-via-one-hop-variable | xss | edge-cases | TP | xss-inner-html | DOMPurify.sanitize() called one step before the sink, through a variable — now recognized as sanitized (severity low) where before the sanitizer call was invisible behind the identifier |
| xss-literal-via-two-hop-variable-still-flagged | xss | edge-cases | TP | xss-inner-html | KNOWN FALSE POSITIVE, documented on purpose: same-scope tracing is exactly one hop — a literal passed through a SECOND variable before the sink is still (incorrectly) flagged |
| xss-react-escaped-text | xss | safe | TN | (none) | React default text interpolation (auto-escaped) |
| xss-iframe-srcdoc-dynamic | xss | vulnerable | TP | xss-iframe-srcdoc | new sink: iframe.srcdoc assigned a dynamic value — renders as a full HTML document, scripts included |
| xss-iframe-srcdoc-static-literal | xss | safe | TN | (none) | new sink, safe case: a fixed literal assigned to iframe.srcdoc must NOT be flagged |
| xss-jquery-html-direct-chain | xss | vulnerable | TP | xss-jquery-html | new sink: $('...').html(dynamic) — direct jQuery-rooted chain |
| xss-jquery-html-cached-object-one-hop | xss | vulnerable | TP | xss-jquery-html | new sink: a jQuery object cached in a variable one hop before .html() — the common $el = $(...) convention |
| xss-jquery-html-dollar-prefixed-param | xss | vulnerable | TP | xss-jquery-html | new sink: a jQuery/jqLite object passed in as a $-prefixed parameter (jQuery plugin / AngularJS 1.x directive link function convention) — unresolvable to a declaration, caught by the naming-convention fallback |
| xss-jquery-html-this-dollar-property-chain | xss | vulnerable | TP | xss-jquery-html | new sink: this.$container.find(...).html(dynamic) — the Backbone.View/jQuery-widget convention of caching a jQuery object as a $-prefixed instance property, then chaining off it |
| xss-jquery-html-static-literal | xss | safe | TN | (none) | new sink, safe case: a fixed literal passed to jQuery .html() must NOT be flagged |
| xss-jquery-html-sanitized | xss | safe | TN | xss-jquery-html | new sink, safe case: jQuery .html() wrapped in DOMPurify.sanitize() must be downgraded to low, not high |
| xss-html-method-on-unrelated-object | xss | safe | TN | (none) | regression: a .html() method on an object that is neither jQuery-rooted nor $-prefixed must NOT be flagged — 'report' is not a jQuery-shaped receiver |
| xss-location-href-javascript-uri-not-covered | xss | edge-cases | TN | (none) | KNOWN GAP, documented on purpose (docs/self-audit-2026-09-03.md §6b): no rule inspects location.href/window.location assignment at all, so a javascript: URI XSS built from user input is currently invisible. Not implemented yet specifically because location.href assignment is an extremely common, almost always legitimate redirect pattern — a naive 'any dynamic value reaching location.href' check would repeat the exact false-positive mistake the reverted import()/require() check made; this needs its own careful, javascript:-prefix-specific scoping before shipping, not a broad check. |
| xss-setattribute-href-javascript-uri-not-covered | xss | edge-cases | TN | (none) | KNOWN GAP, documented on purpose (docs/self-audit-2026-09-03.md §6b): setAttribute('href', ...) is never inspected by any rule, so a javascript: URI built via setAttribute is also currently invisible. Same reasoning as the location.href gap above — setAttribute('href', ...) is used constantly for entirely safe purposes. |
| sqli-concat-db-query | sqli | vulnerable | TP | sqli-dynamic-query | string concatenation directly in the db.query() call |
| sqli-concat-via-one-hop-variable | sqli | vulnerable | TP | sqli-dynamic-query | concatenation built one variable hop before the call — now caught via same-scope tracing (previously a documented false negative; fixed) |
| sqli-concat-via-two-hop-variable-not-caught | sqli | edge-cases | TN | (none) | KNOWN FALSE NEGATIVE, documented on purpose: same-scope tracing is exactly one hop — a value passed through a SECOND variable before reaching the call is still not caught |
| sqli-template-client-query | sqli | vulnerable | TP | sqli-dynamic-query | template literal interpolation into client.query |
| sqli-knex-raw-concat | sqli | vulnerable | TP | sqli-dynamic-query | knex.raw with concatenation |
| sqli-connection-query-concat | sqli | vulnerable | TP | sqli-dynamic-query | connection.query direct concatenation |
| sqli-pool-execute-template | sqli | vulnerable | TP | sqli-dynamic-query | .execute with template interpolation |
| sqli-unsafe-concat | sqli | vulnerable | TP | sqli-dynamic-query | .unsafe with concatenation |
| sqli-parameterized-placeholder | sqli | safe | TN | (none) | parameterized query with $1 placeholder — must NOT flag |
| sqli-parameterized-question-mark | sqli | safe | TN | (none) | parameterized query with ? placeholder — must NOT flag |
| sqli-orm-prisma | sqli | safe | TN | (none) | ORM query builder, no raw SQL at all |
| sqli-orm-active-record-style | sqli | safe | TN | (none) | query-builder .query on non-db-shaped receiver name — not qualified, must NOT flag |
| sqli-sequelize-query-concat | sqli | vulnerable | TP | sqli-dynamic-query | new: sequelize.query() with concatenation — Sequelize raw-query escape hatch |
| sqli-typeorm-datasource-query-template | sqli | vulnerable | TP | sqli-dynamic-query | new: TypeORM DataSource.query() with template interpolation |
| sqli-typeorm-queryrunner-concat | sqli | vulnerable | TP | sqli-dynamic-query | new: TypeORM QueryRunner.query() with concatenation |
| sqli-prisma-query-raw-unsafe-concat | sqli | vulnerable | TP | sqli-dynamic-query | new: Prisma's explicitly-named unsafe raw-query escape hatch, called as a plain function with concatenation |
| sqli-prisma-execute-raw-unsafe-template | sqli | vulnerable | TP | sqli-dynamic-query | new: Prisma $executeRawUnsafe with template interpolation |
| sqli-prisma-query-raw-tagged-template-safe | sqli | safe | TN | (none) | Prisma's $queryRaw used as a TAGGED template literal auto-parameterizes each ${} and is safe by design — must NOT flag. Also structurally a TaggedTemplateExpression, never a CallExpression, so this rule's visitor never even sees it. |
| sqli-typeorm-manager-not-qualified | sqli | edge-cases | TN | (none) | KNOWN FALSE NEGATIVE, documented on purpose: bare 'manager' is deliberately NOT in the qualified-receiver list (too generic — cache/state/task managers commonly expose unrelated .query-shaped methods), so TypeORM's manager.query() with real concatenated SQL is missed here |
| sqli-no-sanitizer-downgrade-path | sqli | edge-cases | TP | sqli-dynamic-query | KNOWN GAP, documented on purpose (docs/self-audit-2026-09-03.md §6b): unlike xss.ts, this rule has NO sanitizer-recognition path at all. Concatenation reaching .query() is always 'high', even when an operand is itself a call to something that looks like a real escaping function — there is no equivalent of xss.ts's isSanitizerWrapped()/severity-downgrade-to-low here. This is arguably the conservative/safe default (an unverified 'escaping' call name proves nothing), but it means this specific case can never be distinguished from a genuinely raw, unescaped concatenation. |
| secret-openai-key | secrets | vulnerable | TP | secret-openai | OpenAI-shaped key literal |
| secret-anthropic-key | secrets | vulnerable | TP | secret-anthropic | Anthropic-shaped key literal |
| secret-stripe-live-key | secrets | vulnerable | TP | secret-stripe | Stripe live secret key |
| secret-aws-access-key | secrets | vulnerable | TP | secret-aws | AWS access key ID |
| secret-github-pat | secrets | vulnerable | TP | secret-github-pat | GitHub PAT |
| secret-generic-password-assignment | secrets | vulnerable | TP | secret-generic-assignment | generic PASSWORD-named variable with a real-looking value |
| secret-env-var-usage | secrets | safe | TN | (none) | proper env-var usage, no literal secret at all |
| secret-placeholder-example | secrets | safe | TN | (none) | documentation placeholder value must be excluded outright |
| secret-firebase-public-config | secrets | edge-cases | TP | secret-google-api-key | intentionally-public Firebase web config key — must be labelled informational, not a leak, and must NOT be mislabeled as a different vendor |
| secret-uuid-not-flagged | secrets | safe | TN | (none) | a bare UUID (e.g. a React key or trace id) must NOT be reported as any vendor secret, and — now that the entropy fallback exists — must not be flagged by that path either (explicit UUID exclusion) |
| secret-entropy-non-credential-name | secrets | vulnerable | TP | secret-high-entropy-string | new: a high-entropy token-shaped value assigned to a name the existing name-context check doesn't recognize as credential-shaped |
| secret-entropy-authorization-header | secrets | vulnerable | TP | secret-high-entropy-string | new: a high-entropy token passed directly as a header value with no named variable at all — invisible to the name-context check by construction |
| secret-entropy-git-sha-not-flagged | secrets | safe | TN | (none) | adversarial: a 40-char pure-hex git SHA-1 must NOT be flagged by the entropy fallback (canonical hash length + pure-hex exclusion) |
| secret-entropy-npm-integrity-not-flagged | secrets | safe | TN | (none) | adversarial: an npm/yarn SRI 'integrity' hash has high entropy but must NOT be flagged (sha512- prefix exclusion) — a real false positive found during calibration, before shipping |
| secret-entropy-cdn-url-not-flagged | secrets | safe | TN | (none) | adversarial: a CDN URL with a hashed filename has entropy close to a real token's but must NOT be flagged (URL-shape exclusion) |
| secret-entropy-base64-image-not-flagged | secrets | safe | TN | (none) | adversarial: a base64 image/font blob must NOT be flagged (falls under the entropy bar in calibration — base64 alphabets skew lower-entropy than true random tokens at this sample length) |
| secret-entropy-long-identifier-not-flagged | secrets | safe | TN | (none) | adversarial: a long, versioned SNAKE_CASE/identifier-style string must NOT be flagged (letter-heavy, under the entropy bar) |
| exec-eval-dynamic | exec | vulnerable | TP | exec-eval | eval on a parameter |
| exec-eval-literal | exec | vulnerable | TP | exec-eval | eval on a fixed literal — still flagged, described accurately |
| exec-new-function | exec | vulnerable | TP | exec-function-ctor | new Function constructor |
| exec-function-call-form | exec | vulnerable | TP | exec-function-ctor | Function(...) called without new |
| exec-set-timeout-string | exec | vulnerable | TP | exec-set-timeout-string | setTimeout with a concatenated string body |
| exec-set-interval-string | exec | vulnerable | TP | exec-set-interval-string | setInterval with a template-literal string body |
| exec-set-timeout-function-ref | exec | safe | TN | (none) | setTimeout with a function reference — must NOT flag |
| exec-no-dynamic-execution | exec | safe | TN | (none) | fixed, non-dynamic function call, no eval-family API at all |
| exec-dynamic-import-nonliteral-not-flagged | exec | safe | TN | (none) | REVERTED CHECK, documented on purpose (see docs/model-improvements.md): import() with a computed specifier was briefly flagged, then removed after live testing showed it fires on the standard route/locale code-splitting idiom — indistinguishable from this via AST shape alone. Must NOT flag. |
| exec-require-nonliteral-not-flagged | exec | safe | TN | (none) | REVERTED CHECK, documented on purpose (see docs/model-improvements.md): require() with a computed specifier (plugin/strategy-loading idiom) — same reversal, must NOT flag. |
| node-exec-dynamic | node-command | vulnerable | TP | node-command-exec-dynamic | exec() with concatenated user input |
| node-exec-sync-dynamic | node-command | vulnerable | TP | node-command-exec-dynamic | execSync() with template literal |
| node-spawn-shell-true-dynamic | node-command | vulnerable | TP | node-command-spawn-shell | spawn with shell:true and dynamic arg |
| node-execfile-fixed-args | node-command | safe | TN | (none) | execFile with fixed command and static args — must NOT flag (different API, out of scope) |
| node-spawn-no-shell | node-command | safe | TN | (none) | spawn without shell:true — must NOT flag |
| react-onclick-handler | react-regression | safe | TN | (none) | standard onClick prop must never be reported as XSS/RCE |
| react-onchange-onsubmit | react-regression | safe | TN | (none) | onChange/onSubmit props must never be reported as XSS/RCE |
| react-usememo-usecallback-correct | react-regression | safe | TN | (none) | idiomatic memoization must not itself be flagged as a security issue |
| react-inline-object-and-onclick | react-regression | safe | TN | (none) | inline object/arrow props with onClick — a performance nit at most, never a critical XSS finding |
| react-dangerously-set-sanitized | react-regression | safe | TN | xss-dangerously-set-inner-html | dangerouslySetInnerHTML wrapped in DOMPurify.sanitize must be low severity, not high/critical |
| auth-hardcoded-password-comparison | auth | vulnerable | TP | auth-hardcoded-credential-comparison | a request password compared directly against a hardcoded literal — a real backdoor/debug-credential shape |
| auth-hardcoded-apikey-comparison | auth | vulnerable | TP | secret-openai, auth-hardcoded-credential-comparison | an API key checked against a literal instead of a configured value (also matches the OpenAI vendor secret pattern -- both rules are expected to fire independently on the same literal) |
| auth-jwt-decode-no-verify | auth | vulnerable | TP | auth-jwt-decode-without-verify | jwt.decode() used with no jwt.verify() anywhere in the file — the signature is never checked |
| auth-password-compared-to-hash-variable | auth | safe | TN | (none) | comparing to a variable (presumably a hash), not a literal — the normal, correct shape — must NOT flag |
| auth-role-compared-to-literal-not-credential | auth | safe | TN | (none) | 'role' is not a credential-shaped name — a normal role check against a literal must NOT flag |
| auth-oauth-grant-type-password | auth | safe | TN | (none) | OAuth2's 'password' grant type — the compared identifier is 'grantType', not a credential-shaped name; this rule checks the NAME being compared, never the literal's content |
| auth-empty-password-check | auth | safe | TN | (none) | checking for an empty password is a normal validation pattern, not a backdoor — empty string literal is explicitly excluded |
| auth-tokentype-bearer-scheme | auth | safe | TN | (none) | 'tokenType' matches the credential-name pattern (contains 'token'), but 'Bearer' is an auth-scheme name, not a secret value — explicitly excluded |
| auth-jwt-verify-used | auth | safe | TN | (none) | jwt.verify() is called elsewhere in the file — jwt.decode() must NOT be flagged when verification happens somewhere in the same file |
| auth-unrelated-decode-not-jwt | auth | safe | TN | (none) | a .decode() call with no 'jsonwebtoken' reference anywhere in the file must NOT be flagged — this isn't a JWT-decoding rule for any decode() call |
| auth-test-assertion-not-flagged | auth | safe | TN | (none) | a test-assertion-library call (expect().toBe()) is a CallExpression, not a BinaryExpression === comparison — must NOT flag, by AST shape alone |
| auth-bcrypt-compare-used | auth | safe | TN | (none) | real bcrypt.compare() usage — a CallExpression, not a === comparison — must NOT flag |
| safe-plain-component | general-safe | safe | TN | (none) | ordinary, unremarkable React component |
| safe-math-utils | general-safe | safe | TN | (none) | plain utility functions, nothing dangerous |
| safe-fetch-json | general-safe | safe | TN | (none) | typical async data-fetching code |
| safe-typescript-interfaces | general-safe | safe | TN | (none) | plain TypeScript types/interfaces, verifies TS syntax parses cleanly |
| safe-class-component | general-safe | safe | TN | (none) | legacy class component with lifecycle methods, no dangerous patterns |

## Failing cases in this run

None — every case in the corpus matches its documented expectation.

## Documented blind spots (intentionally included, not failures)

These cases pass their test (the engine does exactly what it says it does) but exist specifically to keep a real limitation visible instead of only testing favorable cases:

- `xss-sanitizer-via-one-hop-variable` (xss, outcome TP): DOMPurify.sanitize() called one step before the sink, through a variable — now recognized as sanitized (severity low) where before the sanitizer call was invisible behind the identifier
- `xss-literal-via-two-hop-variable-still-flagged` (xss, outcome TP): KNOWN FALSE POSITIVE, documented on purpose: same-scope tracing is exactly one hop — a literal passed through a SECOND variable before the sink is still (incorrectly) flagged
- `xss-location-href-javascript-uri-not-covered` (xss, outcome TN): KNOWN GAP, documented on purpose (docs/self-audit-2026-09-03.md §6b): no rule inspects location.href/window.location assignment at all, so a javascript: URI XSS built from user input is currently invisible. Not implemented yet specifically because location.href assignment is an extremely common, almost always legitimate redirect pattern — a naive 'any dynamic value reaching location.href' check would repeat the exact false-positive mistake the reverted import()/require() check made; this needs its own careful, javascript:-prefix-specific scoping before shipping, not a broad check.
- `xss-setattribute-href-javascript-uri-not-covered` (xss, outcome TN): KNOWN GAP, documented on purpose (docs/self-audit-2026-09-03.md §6b): setAttribute('href', ...) is never inspected by any rule, so a javascript: URI built via setAttribute is also currently invisible. Same reasoning as the location.href gap above — setAttribute('href', ...) is used constantly for entirely safe purposes.
- `sqli-concat-via-two-hop-variable-not-caught` (sqli, outcome TN): KNOWN FALSE NEGATIVE, documented on purpose: same-scope tracing is exactly one hop — a value passed through a SECOND variable before reaching the call is still not caught
- `sqli-typeorm-manager-not-qualified` (sqli, outcome TN): KNOWN FALSE NEGATIVE, documented on purpose: bare 'manager' is deliberately NOT in the qualified-receiver list (too generic — cache/state/task managers commonly expose unrelated .query-shaped methods), so TypeORM's manager.query() with real concatenated SQL is missed here
- `sqli-no-sanitizer-downgrade-path` (sqli, outcome TP): KNOWN GAP, documented on purpose (docs/self-audit-2026-09-03.md §6b): unlike xss.ts, this rule has NO sanitizer-recognition path at all. Concatenation reaching .query() is always 'high', even when an operand is itself a call to something that looks like a real escaping function — there is no equivalent of xss.ts's isSanitizerWrapped()/severity-downgrade-to-low here. This is arguably the conservative/safe default (an unverified 'escaping' call name proves nothing), but it means this specific case can never be distinguished from a genuinely raw, unescaped concatenation.
- `secret-firebase-public-config` (secrets, outcome TP): intentionally-public Firebase web config key — must be labelled informational, not a leak, and must NOT be mislabeled as a different vendor
- `exec-dynamic-import-nonliteral-not-flagged` (exec, outcome TN): REVERTED CHECK, documented on purpose (see docs/model-improvements.md): import() with a computed specifier was briefly flagged, then removed after live testing showed it fires on the standard route/locale code-splitting idiom — indistinguishable from this via AST shape alone. Must NOT flag.
- `exec-require-nonliteral-not-flagged` (exec, outcome TN): REVERTED CHECK, documented on purpose (see docs/model-improvements.md): require() with a computed specifier (plugin/strategy-loading idiom) — same reversal, must NOT flag.

## Methodology notes

- This corpus is intentionally narrow (54 cases across the specific rule groups implemented). It is not a general-purpose vulnerability benchmark and must not be cited as one.
- "TN" (true negative) rows are safe fixtures where no finding at/above the case's `forbidSeverity` threshold appeared, as intended.
- Documented false negatives (e.g. `sqli-concat-via-variable-not-caught`) are included in the corpus on purpose, to keep the engine's real limitations visible in this report rather than hidden by only testing cases it handles well.
- Re-run with `npm run bench` any time a rule changes; this file should be regenerated, not hand-edited.
